import {Typography} from "@mui/material";

const PrivateComponent = () => {
    return (
        <div>
            <Typography>This is a protected component</Typography>
        </div>
    );
};

export default PrivateComponent;